\begin{block}{Status \& Next Milestones}
  \textbf{Done at the platform level.} Local control plane, remote model deployment
  across three GPU tiers, a shared architecture contract, baseline + novel swarm
  architectures, MLflow + report generation, a web UI, and a latency / token / cost /
  energy / emissions measurement stack.

  \vspace{6pt}
  \textbf{Deliberately not over-claimed.} The coding benchmark, human-preference
  workflow, and end-to-end runner are not yet stable, so \emph{no final numbers are
  reported}.

  \vspace{6pt}
  \textbf{Next:}
  \begin{enumerate}
    \item Finalize and validate the \texttt{custom\_stratified} coding benchmark.
    \item Stabilize the experiment runner for repeatable comparative runs.
    \item Complete one valid comparison cycle: monolithic $\to$ routing $\to$
          multi-agent $\to$ ensemble $\to$ swarm family.
    \item Integrate the human-preference workflow into the UI.
  \end{enumerate}
\end{block}
