# Training Adapters

Bu dizin fine-tuned LoRA/QLoRA adapter çıktıları ve `registry.json` için ayrılmıştır.

Adapter ağırlıkları büyük olabileceği için git'e eklenmemelidir. Sadece küçük metadata ve registry dosyaları takip edilmelidir.

Beklenen registry konumu:

```text
training/adapters/registry.json
```

